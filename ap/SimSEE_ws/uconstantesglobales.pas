unit uconstantesglobales;

{$mode delphi}

interface

uses
  Classes, SysUtils;

const
    subcarpeta_raiz = '';
    base_url_tiny_subir_helpers = subcarpeta_raiz + '/adm/';
    base_url_tiny_lists = subcarpeta_raiz + '/adm/lists/';
                                                                        base_url_imagesscar = subcarpeta_raiz+'/adm/imagesscar/';


implementation

end.

