program evaluador;

{$mode objfpc}{$H+}

uses {$IFDEF UNIX} {$IFDEF UseCThreads}
  cthreads, {$ENDIF} {$ENDIF}
  Classes,
  uevaluadormatches;

const
  metodo = 1;

  // 1 usa el resultado actual y los dos siguientes, 2 usa los 5 siguientes y 3 los 9 siguientes
begin
  evaluar(metodo);
end.
{TODO
hacer que de positivo
}
